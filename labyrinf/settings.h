#pragma once

namespace settings{
    const int cell_size(40);
    const int cell_offset_x(2);
    const int cell_offset_y(2);

    const int player_radius(10);
    const int player_offset_x(cell_offset_x + 35.0/4.0);
    const int player_offset_y(cell_offset_x + 35.0/4.0);
}
