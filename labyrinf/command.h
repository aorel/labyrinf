#pragma once

typedef enum Command
{
    PlayerUp,
    PlayerDown,
    PlayerLeft,
    PlayerRight,
} Command;

/*
class Command{
public:
    const Commands command;
    Command(Commands c);
private:
    Command();
};
*/
